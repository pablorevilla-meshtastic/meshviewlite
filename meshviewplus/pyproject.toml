[project]
name = "meshviewplus"
version = "0.1.0"
description = "Minimal scaffold created in workspace"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
meshviewplus = "meshviewplus.main:main"
